# Example data

`opcua_sensor_log.csv` is a minimal event-log sample retained from the original
coursework. It confirms the CSV schema but is not a complete transport-time
experiment and should not be presented as a validated performance result.
